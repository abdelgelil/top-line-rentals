{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd \"client/Top Line\" && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npm install && npm start",
    "healthcheckPath": "/"
  }
}
