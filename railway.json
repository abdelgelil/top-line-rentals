{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd \"Top Line\" && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "caddy run --config /Caddyfile --adapter caddyfile",
    "healthcheckPath": "/"
  }
}
