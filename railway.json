{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "root": "client/Top Line",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "caddy run --config /Caddyfile --adapter caddyfile",
    "healthcheckPath": "/"
  }
}
